{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Java and Sync Git",
            "type": "shell",
            "command": "${command:workbench.action.files.save} && javac ${file} && java ${fileBasenameNoExtension} && git add . && git commit -m \"Auto-sync: ${fileBasename} updated\" && git push",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "presentation": {
                "reveal": "always",
                "panel": "new"
            },
            "problemMatcher": []
        }
    ]
} 